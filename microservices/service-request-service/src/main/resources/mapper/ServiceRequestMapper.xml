<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.sunyesle.microservices.service_request.ServiceRequestMapper">
    <select id="selectAll" resultType="com.sunyesle.microservices.service_request.ServiceRequest">
        SELECT
            tsr.id,
            tsr.title,
            tsr.customer_id,
            tsr.type,
            tsr.detail,
            tsr.status,
            tsr.call_agent_id,
            tsr.voc_assignee_id,
            tsr.voc_assignee_dept_id,
            tsr.created_at,
            tsr.updated_at
        FROM
            tb_service_request tsr
        ORDER BY tsr.updated_at DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>
</mapper>
